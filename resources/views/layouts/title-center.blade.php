<div class="page-header">
    <h1 class="text-center">@hasSection('icon')<i class="fa fa-@yield('icon')" aria-hidden="true"></i>@endif @yield('title') @hasSection('subtitle')<small> - @yield('subtitle')</small>@endif</h1>
</div>